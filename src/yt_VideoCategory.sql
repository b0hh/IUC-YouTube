-- ↓ SQL TABLO GÖRÜNÜMÜ ↓

-- +----+---------------+
-- | ID | category_name |
-- +----+---------------+

CREATE TABLE VideoCategory 
(
    ID INT NOT NULL PRIMARY KEY,
    category_name VARCHAR NOT NULL,
)